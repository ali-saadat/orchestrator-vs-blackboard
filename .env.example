# Copy to `.env` (gitignored) and fill in. The default Mock/Cassette modes need none of this.
#
# For `ovb ... --real` (live streaming Claude calls):
ANTHROPIC_API_KEY=sk-ant-...
#
# Optional — for `ovb serve --ngrok` (public URL). Prefer `--lan` on managed/corporate Macs.
# NGROK_AUTHTOKEN=...
